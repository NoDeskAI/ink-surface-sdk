# Ideation

This directory stores CE idea exploration and option comparison.

Use it for:

- alternative product or technical directions
- MVP shape exploration
- tradeoff analysis
- candidate approaches before a concrete plan exists

When an option is selected, capture the execution path in `docs/plans/`. When an option becomes a reusable practice, promote it into `docs/solutions/`.
