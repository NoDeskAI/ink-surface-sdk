# InkLoop V1 Productization Acceptance Snapshot

Date: 2026-07-04

Scope: Web / Cloud Hub import, InkLoop Paper on M103, Runtime Sync, Obsidian projection, and Lark Meeting Timeline SDK event-mark path.

This snapshot records current implementation evidence against `docs/project/inkloop-eink/01_技术方案/10_产品与PRD_6月方案/InkLoop_v1_产品化定位与三端体验.md`. It is not a final completion claim for the whole product goal.

## Current Evidence

### Latest Rerun: 2026-07-06 14:41 CST

| Area | Evidence | Result |
| --- | --- | --- |
| T10CPlus USB and app target | ADB sees the connected current Paper device as `99CB5B55`, `model=T10CPlus`, `manufacturer=ONYX`, Android `12`; `com.inkloop.app` is foreground/running. Cloud Hub remains fixed at `http://127.0.0.1:8731` | Pass |
| T10 reading mark persistence and Runtime Sync | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb INKLOOP_REQUIRE_TARGET_DOCUMENT=1 INKLOOP_CLEANUP_BEFORE=1 INKLOOP_CLEANUP_AFTER=1 npm --prefix examples/ai-annotation-demo run smoke:paper-reading-mark-types-device` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/paper-reading-mark-types/paper-reading-mark-types-2026-07-06T06-39-31-252Z.json`. It forced target document `AI时代的UX范式.pdf` (`doc_3cfa06ac81d6`), created `underline`, `circle`, `handwriting`, and `review_later`, reopened the book with all 4 marks intact, observed 3 Runtime Sync events for the runtime-eligible marks, and cleaned the 4 synthetic test marks after the run | Pass |
| T10 stylus input profile | `npm --prefix examples/ai-annotation-demo run smoke:paper-stylus-input:synthetic` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/paper-stylus-input/paper-stylus-input-2026-07-06T06-39-57-581Z.json`. The script correctly discovered `/dev/input/event3` as `onyx_emp_Wacom I2C Digitizer` with `ABS_X`, `ABS_Y`, `ABS_PRESSURE`, `BTN_STYLUS`, and direct input props. The production T10 build rejects `sendevent` injection with `Permission denied`, so this is diagnostic only; true T10 physical acceptance requires `smoke:paper-stylus-input:live` while drawing one real stroke | Diagnostic warn |
| Updated product acceptance audit | `npm --prefix examples/ai-annotation-demo run audit:v1-product-acceptance` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-06T06-41-14-786Z.json` with `overall_status=pass`, `6` pass, `0` fail, `1` warn. The new audit includes current T10 reading-mark evidence and a separate T10 stylus gate; it no longer lets historical M103/Huion evidence hide the fact that T10 physical stylus live input is still unproven | Pass with T10 stylus warn |

### Latest Rerun: 2026-07-05 02:20 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Clean final state | Before the final run, Cloud Hub runtime/library/knowledge/device stores, the active test vault `.inkloop`, and the Paper app data were cleared. Cloud Hub was restarted on fixed `http://127.0.0.1:8731`, M103 app `com.inkloop.app/.MainActivity` was relaunched, and Obsidian was opened against `/Users/ethan/Desktop/InkLoop-Obsidian-Test-Vault`. The active vault ended with exactly two managed Markdown files: one Reading note and one Meeting note | Pass |
| Final M103 Reading loop | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb INKLOOP_ACTIVE_OBSIDIAN_VAULT=/Users/ethan/Desktop/InkLoop-Obsidian-Test-Vault npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed on M103 `MBM103YL24L0097`. Web/Cloud import created `doc_inkloop_v1_demo` (`InkLoop V1 Demo.md`); the Paper Library started empty, auto-appeared the document in `4741ms` as `cloud_only`, downloaded/opened it to `synced`, wrote mark `evt_334d3e6c` in `137ms`, reopened with `after_reopen_count=1` and `strokes=1`, and `inkloop://doc/doc_inkloop_v1_demo?anchor=evt_334d3e6c` returned `anchor_found=true` | Pass |
| Reading postprocess and Obsidian projection | The Reading mark reached Runtime Sync as `evt_bridge_evt_334d3e6c`; Cloud Hub persisted `turn_evt_bridge_evt_334d3e6c`, KnowledgeObject `ko_QN15JXRG0XV47ZAYMS71D0KGND`, and projection `dp_doc_inkloop_v1_demo_evt_bridge_evt_334d3e6c`. The active vault contains only `/Users/ethan/Desktop/InkLoop-Obsidian-Test-Vault/InkLoop/Reading/InkLoop V1 Demo/InkLoop V1 Demo.md`, with inline `inkloop:begin-ko`, controlled fields, and `inkloop://doc/...` backlinks instead of separate per-KO notes | Pass |
| Final M103 Meeting loop | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb INKLOOP_ACTIVE_OBSIDIAN_VAULT=/Users/ethan/Desktop/InkLoop-Obsidian-Test-Vault npm --workspace ./examples/ai-annotation-demo run smoke:m103-meeting-device` passed. It created meeting `mtg_887f2a08` and canonical Runtime document `mtgdoc_mtg_887f2a08`, then generated five event marks (`decision`, `action`, `risk`, `question`, `note`) with stable axis counts `1-5` and L1 export kinds `meeting_decision`, `meeting_action`, `meeting_risk`, `qa`, and `annotation` | Pass |
| Meeting postprocess and Obsidian projection | The Meeting run persisted `5` Runtime events, `5` `ai_turns`, `5` postprocess KnowledgeObjects, and `5` projections with `llm_gateway_ok=true`. The active vault contains only `/Users/ethan/Desktop/InkLoop-Obsidian-Test-Vault/InkLoop/Meetings/InkLoop V1 Meeting Demo/InkLoop V1 Meeting Demo.md`, with inline Meeting KOs and postprocess KOs plus `inkloop://doc/mtgdoc_mtg_887f2a08...` backlinks | Pass |
| Paper Wi-Fi/LAN import | After `adb shell svc wifi enable`, M103 reported `wlan0=192.168.7.128`. `smoke:m103-lan-import-formats-device` passed direct LAN upload through fixed port `8787`: the UI showed QR/address, transport was `direct_lan`, missing/wrong token returned `403`, unsupported file type returned `415`, Markdown/PDF/EPUB uploads were local-readable first, then background-synced to Cloud Hub with stable `doc_id`s | Pass |
| Physical pen current-buffer acceptance | The user-drawn M103 buffer `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-current-buffer-2026-07-04T17-06-43-current-buffer.json` contains `5558` RawPenFrames, `55` batches, `7684` hq socket packets, `63` delivered socket strokes, `6297` MotionEvent packets, `56` completed physical strokes, and `input_last_kind=pen`. `npm --workspace ./examples/ai-annotation-demo run evidence:ai-pen-run -- /Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-current-buffer-2026-07-04T17-06-43-current-buffer.jsonl` passed with `5558/5558` valid frames, `54` complete strokes, schema pass rate `1`, p50/p95/p99/max host latency `12ms`, and no open strokes | Pass |
| Acceptance audit after physical-buffer recognition | `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T18-29-24-013Z.json` with `overall_status=pass`, `5` pass, `0` fail, `0` warn. The audit still treats synthetic pen reports as diagnostic-only, but now accepts the real current app buffer when a user has already drawn on the device and the bridge contains validated physical RawPenFrames | Pass |
| Test-run projection isolation | Generic `verify:v1-product` smoke outputs now mark Cloud Hub postprocess records with `metadata.inkloop_test_run=true`, and `inkloop-sync` filters those Cloud Knowledge records before rendering active Obsidian Markdown. Historical untagged smoke docs `doc_v1_ad7b9546531d`, `doc_v1_2421e6bac882`, and `mtgdoc_mtg_v1_sdk_e2e` were removed from Cloud Hub knowledge/library/runtime stores and from `/Users/ethan/Desktop/InkLoop-Obsidian-Test-Vault`. After cleanup, the active vault stayed at exactly two files: the Reading demo and Meeting demo | Pass |
| Re-run without Obsidian pollution | After restarting Cloud Hub on `8731` with the test-run metadata propagation and reinstalling the Obsidian plugin, `npm --workspace ./examples/ai-annotation-demo run verify:v1-product` passed again. The before/after active-vault file list remained unchanged with only `/InkLoop/Reading/InkLoop V1 Demo/InkLoop V1 Demo.md` and `/InkLoop/Meetings/InkLoop V1 Meeting Demo/InkLoop V1 Meeting Demo.md`; the final audit `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T18-38-11-084Z.json` passed with `5` pass, `0` fail, `0` warn | Pass |
| No-device product and SDK checks | `npm --workspace ./examples/ai-annotation-demo run verify:v1-product`, `npm --workspace ./examples/ai-annotation-demo run check`, `npm run build:obsidian-plugin`, and `npm --prefix examples/ai-annotation-demo/Lark-Meeting-Timeline-main run check` all passed. Generic smoke runs no longer mirror into the active desktop vault by default, so repeated verification no longer pollutes Obsidian with new demo notes | Pass |
| Live empty-window caveat | A later `smoke:m103-physical-pen-capture` with a `3000ms` window and no user stroke still produced `no_low_level_stylus_event`; this is expected for an empty capture window and must not override the separately exported real App buffer. Future real-pen live tests still require the operator to draw during the capture window, or to export the current App buffer after drawing | Diagnostic |

### Latest Rerun: 2026-07-05 00:01 CST

| Area | Evidence | Result |
| --- | --- | --- |
| ADB / USB recovery | ADB daemon was restarted after a parallel-start race on `tcp:5037`; M103 reconnected over USB as `MBM103YL24L0097`. `adb root` succeeded, restoring root-level sysfs/hardware visibility. InkLoop was foregrounded as `com.inkloop.app/.MainActivity` | Pass |
| Huion hardware visibility after reconnect | Root shell confirmed `huion-ts` remains `/dev/input/event3` with touch/stylus/eraser/X/Y/pressure capabilities. `/sys/devices/platform/fe5a0000.i2c/i2c-1/1-0008` reported `huion_detect_status=1`, `wacom_cast_open=0`, `work_area_enable=0`, `wakeup_event_count=0`, and `/proc/interrupts` still showed `hgtxx` total `0` | Diagnostic pass |
| Real low-level stylus capture, all devices | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input:live` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-04T15-58-10-099Z.json`: 45s capture saw `huion-ts/event3 line_count=0`; only `gsensor` was active; `hgtxx_interrupt_delta=0` | Not passed |
| Real App RawPenFrame capture after HqHw arm | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_CAPTURE_TIMEOUT_MS=20000 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T15-59-33-867Z.json`: InkLoop was ready (`data-read=book`, visible `#ink-layer=826x720`, `osd_armed=true`, `socket_connected=true`, `sys.is.openhw=1`, `sys.hw.process=1081`, native area `[78,18][1158,1257]`), but `huion-ts/event3 line_count=0`, `hgtxx_interrupt_delta=0`, `socket_packet_count=0`, and `reason=no_low_level_stylus_event` | Not passed |
| Real low-level stylus capture, huion-ts only | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_STYLUS_INPUT_DEVICE=huion-ts M103_STYLUS_INPUT_TIMEOUT_MS=60000 npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-04T16-00-17-083Z.json`: focused 60s capture on `/dev/input/event3` saw no events at all (`line_count=0`, `active_device_count=0`) while `huion_detect_status=1`, `sys.is.openhw=1`, and `hgtxx_delta=0` | Not passed |
| Synthetic downstream sanity check | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_CAPTURE_TIMEOUT_MS=5000 M103_PEN_INJECT_SYNTHETIC=1 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` passed and wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T16-01-27-344Z.json`: synthetic event3 input produced `huion-ts line_count=14`, App bridge produced `17` RawPenFrames, `socket_packet_count=17`, and `socket_delivered_stroke_count=1` | Synthetic pass; not physical acceptance |
| Current product acceptance gate | `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` produced `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T16-01-46-683Z.json`: `3` gates passed and `2` failed. Cloud Hub fixed runtime, active Obsidian plugin, and synthetic diagnostic separation pass; only real physical stylus low-level input and app bridge gates fail | Overall fail only on real physical stylus gates |

### Latest Rerun: 2026-07-04 10:36 CST

| Area | Evidence | Result |
| --- | --- | --- |
| M103 physical pen capture harness readiness fix | `examples/ai-annotation-demo/scripts/verify-m103-physical-pen-capture-device.ts` now separates "enter reading shell" from "close transient overlays". The script clicks the 阅读 navigation only before `openBook()`, then keeps `data-read=book` while clearing `files-open`, `mtg-note-open`, `side-open`, `insight-open`, and `tools-open`; readiness evidence now also records `blocking_overlays` and pen-system before/after state | Pass |
| TypeScript validation | `npm --workspace ./examples/ai-annotation-demo run check` passed after the harness change | Pass |
| Synthetic downstream pen bridge after readiness fix | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_CAPTURE_TIMEOUT_MS=5000 M103_PEN_INJECT_SYNTHETIC=1 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` passed and wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T02-35-32-562Z.json` plus RawPenFrame JSONL `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-raw-pen-2026-07-04T02-35-32-562Z.jsonl`. Readiness was clean: `data-mode=read`, `data-read=book`, `#ink-layer=826x720`, `osd_armed=true`, `blocking_overlays=[]`; synthetic capture produced `17` frames, `socket_packet_count=17`, `socket_delivered_stroke_count=1`, and `synthetic_diagnostic_ok=true` | Synthetic pass; not physical acceptance |
| Real physical pen capture after readiness fix | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_CAPTURE_TIMEOUT_MS=20000 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T02-35-48-263Z.json`. The app was ready (`data-read=book`, visible `#ink-layer=826x720`, `osd_armed=true`, `socket_connected=true`, native area `[78,18][1158,1257]`), but the capture still had `huion-ts/event3 line_count=0`, `hgtxx_interrupt_delta=0`, `socket_packet_count=0`, and `reason=no_low_level_stylus_event` | Not passed; real hardware event still absent |
| Current product acceptance gate | `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` produced `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T02-36-24-308Z.json`: `3` gates passed and `2` failed. Cloud Hub fixed runtime, active Obsidian plugin, and synthetic diagnostic separation pass; only `m103-low-level-physical-stylus` and `m103-physical-pen-app-bridge` fail, both because real `huion-ts` low-level input is absent | Overall fail only on real physical stylus gates |

### Latest Rerun: 2026-07-04 10:26 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Obsidian stale projection cleanup | `inkloop-sync` now archives stale Cloud Knowledge projection files whose frontmatter points to the same `inkloop_document_id` + `inkloop_knowledge_object_id` but whose path is no longer canonical. Historical duplicate notes under `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/InkLoop V1 Product E2E 2026-07-04T02-11-23-707Z/` were reduced to one source hub file plus one canonical KO file; the archived copy is tracked in `/Users/ethan/Desktop/AI-Content-IP-Vault/.inkloop/archived-projections/latest.json` | Pass |
| Active Obsidian plugin after reload | Reinstalled and reloaded `inkloop-sync`; `/Users/ethan/Desktop/AI-Content-IP-Vault/.inkloop/.obsidian-plugin-status.json` reports `status=sync_completed`, namespace `local/local_demo/obsidian_AI-Content-IP-Vault`, `next_cursor=477`, `cursor_blocked_by_conflicts=false`, Cloud Knowledge totals `48 ai_turns`, `78 KnowledgeObjects`, `52 DocumentProjections`, and `100` rendered Markdown files | Pass |
| Full V1 product verification | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run verify:v1-product:paper` passed. This includes TypeScript checks, Runtime Sync, Cloud Hub device session/status, multi-user isolation, controlled writeback, Obsidian reading kind coverage, Web/Cloud Reading E2E, Meeting SDK E2E, M103 Web import formats, M103 LAN/Wi-Fi import formats, M103 Cloud Library mark persistence, M103 reading mark types, and M103 meeting device E2E | Pass |
| Web import formats on M103 | Same full paper verification uploaded Markdown/PDF/EPUB via Cloud Hub first. M103 saw all three documents auto-appear as `cloud_only`, then downloaded all to `synced` with local and cloud availability | Pass |
| M103 Wi-Fi/LAN import formats | Same run verified fixed LAN import on `8787`, visible QR/address, token enforcement (`403` without/wrong token), unsupported type rejection (`415`), direct LAN upload for Markdown/PDF/EPUB, `paper_wifi` source, local-first availability, Cloud Hub background sync, and `wifi_lock_held=true` | Pass |
| M103 Cloud Library reading loop | Same run created `doc_m103_04fb675f4904`, auto-appeared on Paper Library in `2937ms`, opened/downloaded to `synced`, wrote mark `evt_7e176334` in `360ms`, reopened with `after_reopen_count=1` and `strokes=1`, pushed Runtime event `evt_bridge_evt_7e176334` with matching `doc_id`, and mirrored visible Obsidian Reading Markdown | Pass |
| M103 reading mark type coverage | Same run verified `underline`, `circle`, `handwriting`, and `review_later` marks on `8020法则 [美] 理查德·科克.epub`. All four marks survived reopen with one stroke each and entered Runtime Sync as `excerpt`, `annotation`, or `reading_note` under the same document id | Pass |
| M103 meeting device path | Same run created meeting `mtg_3cd4ba0e`, aligned 5 event marks (`decision`, `action`, `risk`, `question`, `note`) to canonical runtime document `mtgdoc_mtg_3cd4ba0e`, persisted 5 Runtime events, 5 `ai_turns`, 5 postprocess KnowledgeObjects, and 5 DocumentProjections, then mirrored 11 Meeting Markdown files into the active Obsidian vault | Pass |
| Current completion audit | `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` produced `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T02-26-25-007Z.json`: Cloud Hub and Obsidian gates pass, but `m103-low-level-physical-stylus` and `m103-physical-pen-app-bridge` still fail because the latest real physical reports have `huion_line_count=0`, `hgtxx_delta=0`, and `socket_packets=0`. Synthetic diagnostics remain explicitly excluded from physical acceptance | Overall fail only on real physical stylus gates |

### Latest Rerun: 2026-07-04 10:15 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Local Cloud Hub availability | Live `curl http://127.0.0.1:8731/healthz` returned HTTP `200` in `0.001520s`; the service reports fixed port `8731`, `local_device_auth=true`, session protection for Library/Runtime/Knowledge/Devices, and durable stores under `examples/ai-annotation-demo/.inkloop/` | Pass |
| Obsidian active vault and plugin | Reinstalled `inkloop-sync` into `/Users/ethan/Desktop/AI-Content-IP-Vault`, reloaded Obsidian, and verified `.inkloop/.obsidian-plugin-status.json`: `status=sync_completed`, namespace `local/local_demo/obsidian_AI-Content-IP-Vault`, `next_cursor=427`, `cursor_blocked_by_conflicts=false`, Cloud Knowledge totals `30 ai_turns`, `50 KnowledgeObjects`, `32 DocumentProjections`, and `66` rendered Markdown files | Pass |
| Runtime Sync cursor conflict fix | `plugins/obsidian/inkloop-sync/main.js` and packaged dist now treat Cloud-only missing-doc `knowledge.update` events the same way as Cloud-only `annotation.add` projections, so stale projection-only events no longer block the Obsidian Runtime cursor. `npm run verify:obsidian-v1-plugin` now asserts this branch explicitly | Pass |
| Cloud Hub LLM and Web import E2E | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` passed in `4733ms`: Web upload created `doc_v1_93a5eaa6dbda`, Library SSE latency was `8ms`, Runtime event `evt_v1_93a5eaa6db` reached sequence `415`, LLM gateway returned `gateway_error=null`, and Cloud Hub persisted `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` | Pass |
| Obsidian visible Markdown projection | The latest V1 Reading projection was written to `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/InkLoop V1 Product E2E 2026-07-04T02-15-26-992Z/`, with exactly two files and a normalized KO filename `这个 V1 Cloud Hub 标记是否需要进入后处理 - evt_v1_9.md`. The KO Markdown contains the `inkloop://doc/doc_v1_93a5eaa6dbda?...` backlink and `inkloop-cloud-mark-layer` SVG, not raw/unrendered mark text | Pass |
| Obsidian filename/rendering parity | `plugins/obsidian/inkloop-sync/main.js`, `dist/obsidian-plugin/inkloop-sync/main.js`, and `packages/adapter-obsidian/src/index.ts` now share NFKC-based filename cleaning and the same `inkloop-cloud-mark-layer` SVG output. This prevents new duplicate files caused by `？` vs removed `?` or old `inkloop-ink` vs plugin-rendered SVG output | Pass |
| M103 Cloud Library mark persistence and Cloud/Runtime doc id alignment | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed on M103 `MBM103YL24L0097` in `14652ms`: Web/Cloud import `doc_m103_662251d5f16e` auto-appeared on Paper Library in `2015ms`, opened/downloaded to `synced`, wrote mark `evt_9b806ac3` in `340ms`, reopened with `after_reopen_count=1` and `strokes=1`, pushed Runtime event `evt_bridge_evt_9b806ac3` with the same `doc_id`, and persisted `turn_evt_bridge_evt_9b806ac3`, `ko_evt_9b806ac3`, and `dp_doc_m103_662251d5f16e_evt_bridge_evt_9b806ac3` | Pass |
| M103 Obsidian projection | Latest M103 run mirrored two Reading Markdown files to `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-04T02-15-36-365Z/`; the KO file contains `inkloop://doc/doc_m103_662251d5f16e?anchor=evt_9b806ac3` and an `inkloop-cloud-mark-layer` path for the mark | Pass |
| Validation commands | `npm run check`, `npm --workspace ./examples/ai-annotation-demo run check`, `npm test -- packages/adapter-obsidian/src/index.test.ts`, and `npm run verify:obsidian-v1-plugin` all passed after the Obsidian plugin and shared adapter changes | Pass |

### Latest Rerun: 2026-07-04 09:56 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Native Reader physical stylus comparison | Opened `/storage/emulated/0/Download/朝花夕拾.pdf` in `com.haoqing.app.hqreader/com.haoqing.ReaderMainActivity`; foreground became `com.haoqing.app.hqreader/com.haoqing.Tab2Activity`, with `sys.hq.HaoQingRunApp=com.haoqing.app.hqreader`, `sys.is.openhw=1`, and `sys.hq.isread=1`. A 45s low-level capture wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-04T01-50-17-973Z.json`: `huion-ts/event3` emitted `0` lines, `hgtxx` interrupt delta stayed `0`, and only `gsensor` was active | Not passed; failure reproduces outside InkLoop |
| Huion sysfs switch experiments | Read `/sys/devices/platform/fe5a0000.i2c/i2c-1/1-0008`: `huion_detect_status=1`, `wacom_detect_status=0` on `1-0009`, `wacom_cast_open=0`, `work_area_enable=0`. Temporarily set `wacom_cast_open=1` and captured `/dev/input/event3` for 20s: no output. Restored it to `0`, then temporarily set `work_area_enable=1` and captured `/dev/input/event3` for 20s: no output. Restored `work_area_enable=0` | Diagnostic only; no physical event |
| System stylus adjustment comparison | Launched `com.haoqing.action.STYLUS_PEN_ADJUSTMENT`; foreground became `com.android.settings/.haoqing.HaoqingStylusPenAdjustmentSettings`, with `sys.hq.HaoQingRunApp=com.android.settings` and `sys.is.openhw=1`. Direct 20s `getevent -lt /dev/input/event3` produced no output | Not passed; failure reproduces in system stylus UI |
| Updated low-level stylus diagnostic wording | `examples/ai-annotation-demo/scripts/verify-m103-stylus-input-device.ts` now treats `huion_line_count=0` + `hgtxx_delta=0` + `huion_detect_status=1` as a pre-InkLoop hardware/driver gate, not a generic retry. Latest report `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-04T01-54-58-228Z.json` says: only `gsensor` emitted events; this is before InkLoop/Runtime Sync; verify physical M103/Huion stylus compatibility / EMR wake in native Reader or stylus adjustment screen | Pass |
| Current product acceptance gate | `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` produced `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T01-55-52-316Z.json`: Cloud Hub fixed runtime, active Obsidian plugin, and synthetic diagnostic separation pass; `m103-low-level-physical-stylus` and `m103-physical-pen-app-bridge` still fail because no real `huion-ts` low-level input reaches the kernel. `npm --workspace ./examples/ai-annotation-demo run check` passed after the diagnostic-script update | Fails only on real physical stylus input |

### Latest Rerun: 2026-07-04 09:47 CST

| Area | Evidence | Result |
| --- | --- | --- |
| APK reinstall with original HWTool pen-down switch | `npm run android:assemble:debug:local-demo` passed and `adb install -r examples/ai-annotation-demo/android/app/build/outputs/apk/debug/app-debug.apk` installed on M103 `MBM103YL24L0097`. `HqHwBridge` now mirrors original `HWTool.automaticallyDrawOpen/Close` by sending `4010,1/0` and exposes `last_pen_down_enable_ok` / `last_pen_down_disable_ok` in `debugStatus()` | Pass |
| M103 Cloud Library mark -> postprocess -> Obsidian after reinstall | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed in `20648ms`: Web/Cloud import `doc_m103_28109af39e98` auto-appeared on Paper Library in `4294ms`, opened/downloaded to local `synced`, wrote mark `evt_9ea5fa6c` in `330ms`, reopened with `after_reopen_count=1` and `strokes=1`, pushed Runtime event `evt_bridge_evt_9ea5fa6c` with the same `doc_id`, persisted one `ai_turn`, one KnowledgeObject, and one projection, then mirrored two Reading Markdown files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-04T01-44-53-833Z/` | Pass |
| Real M103 app bridge capture after `4010` | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_CAPTURE_TIMEOUT_MS=5000 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T01-44-53-828Z.json`: InkLoop was ready with `osd_armed=true`, `last_draw_enable_reply=0`, `last_pen_down_enable_ok=true`, `last_socket_events_enable_ok=true`, `socket_connected=true`, canvas visible, and native area `[78,18][1158,1257]`, but real `socket_packet_count=0` because no low-level stylus event arrived | Not passed |
| Synthetic downstream pen bridge after `4010` | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_CAPTURE_TIMEOUT_MS=5000 M103_PEN_INJECT_SYNTHETIC=1 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` passed and wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T01-45-28-231Z.json` plus RawPenFrame JSONL `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-raw-pen-2026-07-04T01-45-28-231Z.jsonl`. The downstream path saw `huion-ts/event3` injected input, `socket_packet_count=2`, `socket_delivered_stroke_count=1`, and `InputSource.last_kind=pen` | Synthetic pass; physical still not accepted |
| Real M103 low-level stylus input after `4010` | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input:live` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-04T01-45-58-382Z.json`: over the 45s capture window `huion-ts/event3` emitted `0` lines, `hgtxx` interrupt delta stayed `0`, `huion_detect_status=1`, `wacom_cast_open=0`, `work_area_enable=0`, and only `gsensor` was active | Not passed |
| Current product acceptance gate | `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` produced `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T01-46-59-882Z.json`: fixed Cloud Hub, active Obsidian plugin, and synthetic diagnostic separation pass; only `m103-low-level-physical-stylus` and `m103-physical-pen-app-bridge` fail, both because real `huion-ts` input is absent despite `draw_enable_reply=0`, `pen_down_enable=true`, `socket_events_enable=true`, and `socket_connected=true` | Fails only on real physical stylus input |

### Latest Rerun: 2026-07-04 09:35 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Fixed Cloud Hub availability and latency | Live `curl http://127.0.0.1:8731/healthz` returned HTTP `200` in `0.001026s`. The response still reports fixed port `8731`, `local_device_auth=true`, and session protection for Library/Runtime/Knowledge/Devices, with durable stores under `examples/ai-annotation-demo/.inkloop/` | Pass |
| Web import -> Cloud Hub -> Runtime Sync -> postprocess -> Obsidian | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` passed in `15455ms`: Web upload created `doc_v1_840e08759df5`, Library SSE latency was `11ms`, Runtime event sequence advanced to `379`, LLM gateway returned `gateway_error=null`, Cloud Hub persisted exactly `1` `ai_turn`, `1` KnowledgeObject, and `1` DocumentProjection, and active vault `/Users/ethan/Desktop/AI-Content-IP-Vault` received two visible Reading Markdown files with `inkloop://doc/...` backlink and KO content | Pass |
| Meeting SDK event-mark path | `npm --workspace ./examples/ai-annotation-demo run smoke:meeting-v1-e2e` passed in `293ms`: Lark Meeting Timeline SDK accepted `4` meeting annotations, aligned all `4` event marks to InkLoop schema, persisted `4` meeting `ai_turns`, `4` event KnowledgeObjects, `4` postprocess KnowledgeObjects, and `1` meeting projection, then mirrored `9` Meeting Markdown files into the active Obsidian vault | Pass |
| Runtime Sync cursor namespace and controlled writeback | `npm --workspace ./examples/ai-annotation-demo run smoke:runtime-sync-flow` passed in `16ms`: Web -> Obsidian pulled `evt_smoke_web_pen` with `next_cursor=1`, Obsidian -> Web pulled with `next_cursor=3`, skipped its own earlier event, and preserved controlled `knowledge.update` fields (`status=archived`, tags, `task_done=true`) | Pass |
| Obsidian Reading projection coverage | `npm --workspace ./examples/ai-annotation-demo run smoke:obsidian-reading-kind-coverage` passed in `14ms`: Reading projection rendered all five V1 kinds (`reading_note`, `highlight`, `task`, `decision`, `risk`) into both the deterministic test vault and `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/...`, with controlled task/risk/highlight fields parsed back | Pass |
| Active Obsidian plugin package | `npm run verify:obsidian-v1-plugin` passed: source/dist manifests, `main.js`, `styles.css`, packaged SDK IIFE, required V1 runtime text, stale-text absence, and temp-vault installer/settings smoke all passed. The active desktop vault plugin exists at `/Users/ethan/Desktop/AI-Content-IP-Vault/.obsidian/plugins/inkloop-sync`, is enabled, and points Runtime/Knowledge endpoints at `http://127.0.0.1:8731` | Pass |
| M103 Cloud Library mark -> postprocess -> Obsidian | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed in `16181ms`: Web/Cloud import `doc_m103_b8635c6b97fe` auto-appeared on Paper Library in `1692ms` as `cloud_only`, opened/downloaded to local `synced`, wrote mark `evt_b66921d2` in `365ms`, reopened with `after_reopen_count=1` and `strokes=1`, pushed Runtime event `evt_bridge_evt_b66921d2` with the same `doc_id`, persisted `turn_evt_bridge_evt_b66921d2`, KnowledgeObject `ko_KPGFTC8K405X8Q0501CZR9G9FE`, and projection `dp_doc_m103_b8635c6b97fe_evt_bridge_evt_b66921d2`, then mirrored two Reading Markdown files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-04T01-36-33-439Z/` | Pass |
| Native reader comparison on M103 | Switching foreground to `com.haoqing.app.hqreader/.view.activity.MainActivity` showed the native reader bookshelf does not arm handwriting (`sys.is.openhw=0`) and did not change Huion sysfs (`wacom_cast_open=0`, `work_area_enable=0`). Switching back to InkLoop restored `sys.hq.HaoQingRunApp=com.inkloop.app`, `sys.is.openhw=1`, and `sys.hw.process=3847` | Diagnostic only |
| Real M103 physical stylus input | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input:live` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-04T01-33-48-531Z.json`: over the 45s capture window `huion-ts/event3` emitted `0` lines, `hgtxx` interrupt delta stayed `0`, `huion_detect_status=1`, `wacom_cast_open=0`, `work_area_enable=0`, and only `gsensor` was active | Not passed |
| Real M103 app bridge capture | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_CAPTURE_TIMEOUT_MS=5000 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T01-33-48-530Z.json`: InkLoop was ready with `osd_armed=true`, `last_draw_enable_reply=0`, `last_socket_events_enable_ok=true`, `socket_connected=true`, canvas visible, and native area `[78,18][1158,1257]`, but real `socket_packet_count=0` because no low-level stylus event arrived | Not passed |
| Synthetic downstream pen bridge and latency | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_PEN_INJECT_SYNTHETIC=1 npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` passed and wrote `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T01-34-42-658Z.json` plus RawPenFrame JSONL `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-raw-pen-2026-07-04T01-34-42-658Z.jsonl`. `evidence:ai-pen-run` validated `17/17` frames, one complete stroke, and host latency p50/p95/max `12ms` | Synthetic pass; physical still not accepted |
| Current product acceptance gate | `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` produced `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T01-32-50-238Z.json`: Cloud Hub and Obsidian gates pass; only `m103-low-level-physical-stylus` and `m103-physical-pen-app-bridge` fail, both because real `huion-ts` input is absent | Fails only on real physical stylus input |

### Latest Rerun: 2026-07-04 09:27 CST

| Area | Evidence | Result |
| --- | --- | --- |
| M103 HqHw diagnostics in APK | `HqHwBridge.debugStatus()` now includes `last_draw_enable_reply`, `last_socket_events_enable_ok`, `last_socket_events_disable_ok`, `last_native_area`, and `last_viewport_area`. `npm run android:assemble:debug:local-demo` built successfully, the APK was installed on M103 `MBM103YL24L0097`, and `com.inkloop.app` restarted as process `3847` | Pass |
| App-side fast-ink readiness | Short real capture `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T01-26-09-231Z.json` shows `osd_armed=true`, `last_draw_enable_reply=0`, `last_socket_events_enable_ok=true`, `socket_connected=true`, current viewport area `[147,78][1386,1158]`, native area `[78,18][1158,1257]`, and `socket_packet_count=0` | App armed; no real pen packets |
| Synthetic downstream bridge after diagnostics change | Synthetic run `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-physical-pen-capture-2026-07-04T01-26-24-870Z.json` passed with `captured_synthetic_pen_frames`, `event3` low-level input seen, `socket_packet_count=2`, `socket_delivered_stroke_count=1`, `InputSource.last_kind=pen`, and RawPenFrame JSONL written to `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-raw-pen-2026-07-04T01-26-24-870Z.jsonl` | Synthetic pass; physical still not accepted |
| V1 acceptance audit with stronger failure details | Latest `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` produced `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T01-27-03-027Z.json`: Cloud Hub and Obsidian gates pass; `m103-low-level-physical-stylus` fails with `huion_line_count=0`, `hgtxx_delta=0`, only `gsensor` active; `m103-physical-pen-app-bridge` fails with `draw_enable_reply=0`, `socket_events_enable=true`, `socket_connected=true`, `socket_packets=0` | Fails only on real physical stylus input |

### Latest Rerun: 2026-07-04 09:21 CST

| Area | Evidence | Result |
| --- | --- | --- |
| V1 acceptance audit gate | Added `npm --workspace ./examples/ai-annotation-demo run audit:v1-product-acceptance` plus root forwarding script `npm run demo:audit:v1-product-acceptance`. The audit reads live Cloud Hub health, the active Obsidian vault plugin/settings, and latest real-vs-synthetic M103 stylus reports. Latest report is `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/v1-product-acceptance/v1-product-acceptance-2026-07-04T01-21-19-562Z.json` | Added; currently fails only on physical stylus gates |
| Cloud Hub and Obsidian gates in acceptance audit | Same audit passed fixed Cloud Hub `http://127.0.0.1:8731/healthz` with session protection for Library/Runtime/Knowledge/Devices, and passed active Obsidian vault `/Users/ethan/Desktop/AI-Content-IP-Vault` with enabled `inkloop-sync` plugin pointing Runtime push/pull and Knowledge endpoints at `http://127.0.0.1:8731` | Pass |
| Real M103 low-level stylus capture | Ran `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input:live` on connected M103 `MBM103YL24L0097`. Latest report `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-04T01-20-22-151Z.json` still has `physical_acceptance_ok=false`, `reason=observed_non_stylus_input_only`, `huion_line_count=0`, `hardware_hgtxx_interrupt_delta=0`, and only `gsensor` active | Not passed |
| Synthetic-vs-physical separation | The new acceptance audit explicitly passes `synthetic-pen-not-acceptance`: synthetic reports can prove the downstream listener/bridge path, but they cannot satisfy `m103-low-level-physical-stylus` or `m103-physical-pen-app-bridge`. This prevents the V1 completion claim from being made from WebView synthetic pointer or injected `/dev/input/event3` evidence | Pass |

### Latest Rerun: 2026-07-04 09:13 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Fixed local Cloud Hub runtime | Live `http://127.0.0.1:8731/healthz` reports `inkloop-cloud-hub`, fixed port `8731`, `local_device_auth=true`, and Library/Runtime/Knowledge/Devices all require session auth. The running stores are `.inkloop/library`, `.inkloop/runtime-events.jsonl`, `.inkloop/knowledge`, and `.inkloop/devices` under `examples/ai-annotation-demo` | Pass |
| Cloud Hub LLM gateway aligned to Kickstarter/provider config | `.env.example` now documents the NodeSK gateway route as `https://llm-gateway-api.nodesk.tech/default/v1`, `LLM_GATEWAY_TRANSPORT=chat_completions`, and `LLM_MODEL=glm-5.2`; standalone Cloud Hub still maps local `NODESK_API_KEY` to `LLM_GATEWAY_KEY` without committing the key. Latest real-device meeting smoke returned `llm_gateway_ok=true`; latest V1 product run returned `gateway_error=null` | Pass |
| Runtime Sync meeting doc id unification | `runtimeDocumentIdForSyncRequest()` now maps `mtgboard_*` to canonical `mtgdoc_*` before Runtime Sync snapshots are built. `smoke:m103-meeting-device` verified all 5 meeting events used one Runtime document id, `mtgdoc_mtg_bb26a118`, instead of splitting between board and document ids | Pass |
| Cloud Knowledge concurrent persistence | `JsonCloudKnowledgeStore` now serializes namespace writes for `ai_turn`, KnowledgeObject, patches, and DocumentProjection. Regression coverage writes 8 `ai_turns`, 8 KnowledgeObjects, and 8 projections concurrently and verifies none are lost | Pass |
| M103 Paper meeting marks through postprocess to Obsidian | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-meeting-device` passed on M103 `MBM103YL24L0097`: 5 synthetic meeting event marks (`decision`, `action`, `risk`, `question`, `note`) entered Runtime Sync, persisted as 5 `ai_turns`, 5 KnowledgeObjects, and 5 DocumentProjections, and rendered 11 Markdown files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Meetings/2026-07-04 InkLoop Paper Meeting Device E2E 2026-07-04T01 10 53.403Z/` | Pass |
| M103 Web/Cloud Library mark persistence and Obsidian projection | `smoke:m103-cloud-library-device` passed after switching the app back to Reading before opening the imported book: Web import auto-appeared in `1501ms`, device mark write/reopen persistence was `401ms`, Runtime event `evt_bridge_evt_3e8be36e` kept `doc_id=doc_m103_9cb1b6822f90`, Cloud Hub persisted one `ai_turn`, one KnowledgeObject, and one projection, and rendered visible Reading Markdown into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-04T01-13-00-449Z/` | Pass |
| Obsidian plugin and visible Markdown projection | `npm run verify:obsidian-v1-plugin` passed. `npm --workspace ./examples/ai-annotation-demo run verify:v1-product` also passed and mirrored rendered Reading and Meeting outputs into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/...`, so the active desktop vault is no longer the old iCloud test vault path for this flow | Pass |
| Reading mark types survive reopen | `smoke:m103-reading-mark-types-device` passed on source document `doc_2dded0a11122`: `underline`, `circle`, `handwriting`, and `review_later` marks all survived book reopen with strokes intact and all reached Cloud Hub Runtime Sync as `annotation.add` events under the same document id | Pass |
| Physical handwriting latency caveat | Current measured Paper mark persistence uses synthetic WebView pointer/dev hooks and persisted mark replay. The M103/T10C real physical stylus low-latency/OSD path remains a separate hardware acceptance item; synthetic bridge diagnostics are useful, but they do not prove true pen-to-ink latency | Open |

### Latest Rerun: 2026-07-04 08:51 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Android Paper APK with latest mobile runtime | `npm run android:assemble:debug:local-demo` rebuilt the Vite mobile assets and Android debug APK, `verify:android-paper-assets` passed, `adb install -r examples/ai-annotation-demo/android/app/build/outputs/apk/debug/app-debug.apk` installed on M103 `MBM103YL24L0097`, and the app restarted as process `2769` | Pass |
| Full V1 product verification on M103 | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run verify:v1-product:paper` passed end to end after the reinstall. It covered TypeScript checks, Runtime Sync cursor flow, Cloud Hub session/device/multi-user isolation, controlled writeback persistence, five-kind Obsidian Reading projection, V1 Reading E2E, Meeting E2E, M103 Web import formats, M103 LAN import formats, M103 Cloud Library mark/postprocess/Obsidian, and M103 reading mark type persistence | Pass |
| Cloud Hub LLM gateway and persistent postprocess | Latest V1 E2E created `doc_v1_e340fbaa4ec8`, reached Runtime Sync sequence `319`, returned `gateway_error=null`, and persisted one `ai_turn`, one KnowledgeObject, and one DocumentProjection. Latest M103 Cloud Library run created `doc_m103_ecec6c9594a2`, pushed Runtime event `evt_bridge_evt_cc3d2684`, returned `gateway_error=null`, persisted the same triplet, and mirrored rendered Reading Markdown into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-04T00-51-41-685Z/` | Pass |
| Obsidian plugin verification | `npm run verify:obsidian-v1-plugin` passed after the paper verification run: source/dist manifests, `main.js`, `styles.css`, packaged SDK IIFE, V1 runtime text checks, stale-text absence, installer smoke, enabled plugin, and V1 runtime settings in a temp vault all passed | Pass |
| M103 Web/Cloud-first and Paper LAN import coverage | Web import smoke uploaded Markdown/PDF/EPUB through Cloud Hub first; all appeared in Paper Library as `cloud_only` in `2458ms` and downloaded to `synced`. LAN import smoke used fixed port `8787`, visible QR/URL, direct LAN transport, no/wrong token `403`, unsupported type `415`, three uploads `200`, local-first availability in `5843ms`, and final Cloud Hub sync in `2766ms` with stable `doc_id`s | Pass |
| M103 reading mark type persistence | Added `window.__inkloop.createSyntheticReadingMark(kind)` as a dev-only Paper hook and `smoke:m103-reading-mark-types-device`, then wired it into `verify:v1-product:paper`. Latest M103 run created and reopened four mark types on `doc_2dded0a11122`: `underline` (`evt_aaa5a2ce`), `circle` (`evt_40fabc65`), `handwriting` (`evt_31182e70`), and `review_later` (`evt_5854875b`); all kept one stroke after reopening and all appeared in Cloud Hub Runtime Sync as `annotation.add` events with the same `doc_id` | Pass |

### Latest Rerun: 2026-07-04 08:42 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Reading projection kind coverage | Added `npm --workspace ./examples/ai-annotation-demo run smoke:obsidian-reading-kind-coverage` and wired it into `verify:v1-product`. The smoke rendered exactly five required Reading knowledge kinds (`reading_note`, `highlight`, `task`, `decision`, `risk`) plus the source-file hub into both `test-results/obsidian-reading-kind-coverage-vault` and `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/InkLoop Reading Kind Coverage 2026-07-04T00 42 52.393Z/` | Pass |
| Reading controlled field parsing | Same smoke verified Obsidian controlled fields for Reading outputs: task checkbox parsed back as `task_done=true`, risk status/note parsed back as `risk_status=mitigated` and `risk_note="Backup route is ready."`, and highlight comment parsed back as `comment_md="Keep for demo narration."` | Pass |
| Product verification with Reading kind coverage | `npm --workspace ./examples/ai-annotation-demo run verify:v1-product` passed after adding the new smoke. Latest run also reconfirmed Runtime Sync cursor flow, Cloud Hub session/device/multi-user isolation, controlled Cloud Hub writeback persistence, Web/Cloud-first Reading E2E (`doc_v1_22bb7eee8f4c`, SSE `11ms`, runtime sequence `308`, `gateway_error=null`), and Lark Meeting Timeline E2E | Pass |

### Latest Rerun: 2026-07-04 08:37 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Product verification entrypoints | Added `npm run verify:v1-product` and `npm run verify:v1-product:paper` in `examples/ai-annotation-demo/package.json`, with root forwarding scripts `npm run demo:verify:v1-product` and `npm run demo:verify:v1-product:paper`. README now distinguishes no-device product verification, M103 product verification, single M103 book/mark smoke, and synthetic-vs-real pen diagnostics | Pass |
| No-device V1 product verification | `npm --workspace ./examples/ai-annotation-demo run verify:v1-product` passed. It ran TypeScript checks, Runtime Sync cursor flow, Cloud Hub session binding, Cloud Hub DeviceManifest, multi-user isolation, controlled Obsidian writeback persistence, Web/Cloud-first Reading E2E, and Lark Meeting Timeline E2E. Latest Reading run created `doc_v1_d84646496d7d`, Library SSE latency was `9ms`, Runtime event sequence was `301`, LLM gateway returned `gateway_error=null`, and Cloud Hub persisted exactly one `ai_turn`, KnowledgeObject and DocumentProjection | Pass |
| Obsidian plugin package verification | `npm run verify:obsidian-v1-plugin` passed after the product verification run. It verified source/dist manifests, source/dist `main.js` and `styles.css`, packaged SDK IIFE, required V1 runtime text, stale text absence, and temp-vault installer/settings smoke | Pass |
| Obsidian active vault projection | The latest Reading projection mirrored two Markdown files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/InkLoop V1 Product E2E 2026-07-04T00-36-59-832Z/`. Meeting projection mirrored nine files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Meetings/2026-07-03 V1 SDK Meeting E2E/`, with visible `source_refs` and persisted Cloud Hub objects used as the render source | Pass |
| M103 Web/Cloud-first import verification | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run verify:v1-product:paper` passed. The Web import format segment uploaded Markdown, PDF and EPUB through Cloud Hub first; all three appeared in M103 Library as `cloud_only` in `4112ms` and downloaded to local `synced` | Pass |
| M103 Paper Wi-Fi/LAN import verification | Same full verification passed the M103 LAN segment: fixed port `8787`, QR/URL visible, same-Wi-Fi direct LAN transport, no/wrong token `403`, unsupported type `415`, Markdown/PDF/EPUB upload `200`, local-first availability in `9312ms`, and final Cloud Hub sync in `2470ms` with stable `doc_id`s | Pass |
| M103 Library mark persistence and postprocess | Same full verification passed the final M103 book/mark segment: Web-imported `doc_m103_709d577170f5` auto-appeared in `2071ms`, opened/downloaded to local `synced`, wrote mark `evt_78c267a7` in `491ms`, reopened with `after_reopen_count=1` and `strokes=1`, pushed Runtime event `evt_bridge_evt_78c267a7` with the same Cloud Library `doc_id`, persisted exactly one `ai_turn`, KnowledgeObject and DocumentProjection, mirrored Reading Markdown into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-04T00-37-26-719Z/`, and `inkloop://doc/doc_m103_709d577170f5?anchor=evt_78c267a7` returned `anchor_found=true` | Pass |
| Runtime pull smoke false negative fixed | The first `verify:v1-product:paper` run exposed a smoke bug, not a product data-loss bug: `evt_bridge_evt_6232bf5d` for `doc_m103_8723ea2dbd1d` existed in `.inkloop/runtime-events.jsonl` at sequence `296`, and Cloud Knowledge had already persisted `turn_evt_bridge_evt_6232bf5d`, `ko_evt_6232bf5d`, and the document projection. The old M103 smoke pulled only `cursor=0&limit=250`, so it missed newer records. `verify-m103-cloud-library-device.ts` now pages Runtime Sync pulls until `has_more=false`, and the full product paper verification passes | Pass |

### Latest Rerun: 2026-07-04 08:23 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Fixed Cloud Hub runtime | Live `http://127.0.0.1:8731/healthz` reports `inkloop-cloud-hub`, fixed port `8731`, `local_device_auth=true`, and Library/Runtime/Knowledge/Devices all require session auth | Pass |
| Cloud Hub session and device status | `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-device-status` passed with session-authenticated heartbeat, `lan_import.port=8787`, `wifi_lock_held=true`, and persisted state after restart. `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-device-session` passed with QR proxy create/status/ack, shared-secret panel calls, spoofed header rejection, and Library/Runtime/Knowledge records surviving restart | Pass |
| Cloud Hub multi-user isolation | `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-multi-user` now starts a temporary session-required Cloud Hub with mock Panel Auth instead of relying on spoofable tenant/user headers. It passed with `auth_mode=session`: user A and user B could use the same `doc_id`, `event_id`, `ai_turn_id`, `ko_id`, and `projection_id` without cross-stream/blob/runtime/knowledge leakage; user C saw `0` records | Pass |
| Web/Cloud-first import format coverage on M103 | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-web-import-formats-device` passed on M103 before the later ADB disconnect. Markdown/PDF/EPUB were uploaded through Cloud Hub first with run-scoped `doc_id`, appeared in Paper Library as `cloud_only`, `local_available=false`, `cloud_available=true` in `3457ms`, then each downloaded to local `synced` | Pass |
| Paper Wi-Fi/LAN local-first import format coverage | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-lan-import-formats-device` passed with fixed `8787`, token URL and QR visible, no/wrong token `403`, unsupported type `415`, direct LAN transport, Markdown/PDF/EPUB upload `200`, local-first availability in `4729ms`, and final Cloud Hub sync in `3008ms`. The script now makes PDF/EPUB bytes run-scoped so old local cache cannot satisfy the assertion | Pass |
| Integrated V1 reading + meeting E2E | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` passed in `6359ms`: Web import created `doc_v1_5da927bc4897`, Library SSE latency was `11ms`, Runtime event sequence `284` was pulled by the Obsidian device, LLM gateway had `gateway_error=null`, and Cloud Hub persisted exactly one `ai_turn`, KnowledgeObject, and DocumentProjection for the reading mark. The same run also executed the Lark Meeting Timeline E2E: `4` meeting annotations, SSE latency `4ms`, `4` aligned events, `4` postprocess document refs, `4` meeting mark refs, `1` project memory ref, `4` persisted meeting `ai_turns`, `4` postprocess KnowledgeObjects, `1` meeting projection, and active-vault Obsidian files with visible `source_refs` | Pass |
| M103 reading mark, postprocess, and source return | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed on M103 `MBM103YL24L0097`: Web-imported `doc_m103_92838ddc86bd` auto-appeared in `1231ms`, opened as local `synced`, wrote mark `evt_176221f5` in `383ms`, reopened with `after_reopen_count=1`, restored `strokes=1`, pushed Runtime event `evt_bridge_evt_176221f5`, persisted exactly one `ai_turn`, KnowledgeObject and DocumentProjection, mirrored Reading Markdown to `/Users/ethan/Desktop/AI-Content-IP-Vault`, and `inkloop://doc/doc_m103_92838ddc86bd?anchor=evt_176221f5` returned `anchor_found=true` | Pass |
| Runtime Sync and Obsidian package | `npm --workspace ./examples/ai-annotation-demo run smoke:runtime-sync-flow` passed in `11ms` with Web -> Obsidian and Obsidian -> Web controlled `knowledge.update` flow and `release_path_used=false`. `npm run verify:obsidian-v1-plugin` passed source/dist manifests, packaged SDK IIFE, V1 runtime text, stale-text absence, and temp-vault install/settings smoke | Pass |
| Cloud Hub controlled Obsidian writeback persistence | `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-controlled-writeback` now starts a temporary session-required Cloud Hub, authorizes an Obsidian device session, writes a `meeting_risk` KnowledgeObject, pushes a Runtime `knowledge.update` event from `obsidian_controlled_fields`, waits for Cloud Knowledge patch side effects, restarts Cloud Hub, and verifies `status=edited`, tags, `task_done=true`, `risk_status=mitigated`, `risk_note`, and `comment_md` remain persisted after restart | Pass |
| TypeScript and Lark SDK checks | `npm --workspace ./examples/ai-annotation-demo run check` passed after the smoke script fixes. `npm run check --prefix examples/ai-annotation-demo/Lark-Meeting-Timeline-main` passed its alignment, device ingest, annotation routing, passive scan, OAuth/scope, event log, raw WS diagnostic, and meeting search contract suite | Pass |
| Low-latency pen bridge synthetic diagnostic | After restoring ADB and enabling `adb root`, `npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-synthetic` passed with `synthetic_diagnostic_ok=true`, `physical_acceptance_ok=false`, `17` RawPenFrames, `socket_packet_count=17`, `socket_delivered_stroke_count=1`, and output `/Users/ethan/AI-Annotation-demo/examples/ai-annotation-demo/test-results/m103-physical-pen-capture/m103-raw-pen-2026-07-04T00-26-21-785Z.jsonl`. `npm --workspace ./examples/ai-annotation-demo run evidence:ai-pen-run -- <raw-jsonl>` passed with `17/17` valid frames, one complete stroke, and host latency p50/p95/p99/max all `12ms` | Synthetic pass; real physical stylus still not accepted |

### Latest Rerun: 2026-07-04 07:58 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Cloud Hub DeviceManifest contract | Added `/v1/devices/heartbeat`, `/v1/devices/manifest`, and `/v1/devices/:device_id` on the fixed Cloud Hub. `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-device-status` passed: unauthenticated device manifest returned `401`, local device auth created a session, heartbeat used the authenticated session `tenant_id/user_id/device_id` instead of spoofed headers/body, persisted LAN import status (`port=8787`, `wifi_lock_held=true`), and survived Cloud Hub restart | Pass |
| Android Paper device heartbeat | `npm run android:assemble:debug` rebuilt the Paper APK with the new heartbeat code, synced Android assets, and produced `examples/ai-annotation-demo/android/app/build/outputs/apk/debug/app-debug.apk`. The final APK was installed on M103 `MBM103YL24L0097`, the app was restarted, and the process came up as `com.inkloop.app` | Pass |
| Live M103 DeviceManifest state | After final reinstall and a fresh M103 E2E run, querying live `http://127.0.0.1:8731/v1/devices/manifest` with the local session returned `device_count=1` for `device_id=m103-real-device-e2e`, `platform=android-webview`, `status=online`, `app_surface=paper-runtime-host`, `lan_port=8787`, latest `document_id=doc_m103_39065a0693aa`, and `reason=interval` without exposing the session token | Pass |
| M103 chain after APK reinstall | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed after installing the final APK: Web-imported `doc_m103_39065a0693aa` auto-appeared in `989ms`, opened/downloaded to local `synced`, wrote mark `evt_5fc1eb11` in `330ms`, reopened with `after_reopen_count=1`, restored `strokes=1`, and `inkloop://doc/doc_m103_39065a0693aa?anchor=evt_5fc1eb11` returned `anchor_found=true` | Pass |
| Checks for DeviceManifest changes | `npm --workspace ./examples/ai-annotation-demo run check` passed; `npm --workspace ./examples/ai-annotation-demo test -- --run server/cloud-device-store.test.ts server/cloud-device-handler.test.ts` passed `2/2` tests | Pass |

### Latest Rerun: 2026-07-04 07:49 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Fixed local Cloud Hub and provider wiring | Live `http://127.0.0.1:8731/healthz` reports `inkloop-cloud-hub`, fixed port `8731`, and Library/Runtime/Knowledge `require_session=true`. Hermes provider config exists locally with `custom:nodesk-gateway`, `glm-5.2`, OpenAI chat-completions route, and `NODESK_API_KEY` present outside the repo. A live `/api/classify-context` probe returned HTTP `200`, `respond=true`, and `latency_ms=2020` | Pass |
| Active desktop Obsidian vault | `/Users/ethan/Desktop/AI-Content-IP-Vault/.obsidian/plugins/inkloop-sync` is installed/enabled, settings point to fixed Runtime and Knowledge endpoints on `http://127.0.0.1:8731`, and `obsidian.json` contains `/Users/ethan/Desktop/AI-Content-IP-Vault` as the open vault. Current Reading and Meeting projection files are present under `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/...` | Pass |
| V1 Web import -> Runtime Sync -> Cloud Knowledge -> Obsidian | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` passed in `6362ms`: Web/Cloud Hub uploaded `doc_v1_0139a86907c8`, source `web`, SSE manifest latency `9ms`, Runtime event `evt_v1_0139a86907` reached server sequence `266`, Obsidian-device pull received it with `next_cursor=266`, LLM gateway had `gateway_error=null`, and Cloud Hub persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` | Pass |
| Active Obsidian Reading Markdown rendering | Same V1 E2E rendered `2` Reading files into the deterministic test vault and mirrored them into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/InkLoop V1 Product E2E 2026-07-03T23-48-55-050Z/...`; `active_vault_plugin_installed=true`, `contains_backlink=true`, and `contains_visible_ko_content=true` | Pass |
| M103 Web import auto-refresh, doc id alignment, and mark persistence | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed on M103 `MBM103YL24L0097`: Web-imported `doc_m103_dfb72a7818ca` auto-appeared in Paper Library in `920ms` as `cloud_only`, opened/downloaded to `sync_status=synced`, wrote mark `evt_561ea4b4` in `365ms`, reopened with `after_reopen_count=1`, restored `strokes=1`, and `inkloop://doc/doc_m103_dfb72a7818ca?anchor=evt_561ea4b4` returned `anchor_found=true` | Pass |
| M103 mark post-processing persistence | Same M103 run pushed Runtime event `evt_bridge_evt_561ea4b4` with `doc_id=doc_m103_dfb72a7818ca` matching the Cloud Library document id, then persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection`. This specific mark was a pure drawing mark with empty `marked_text`, so `llm_respond=false` and the result is inbox-style, but `gateway_error=null` and the Obsidian projection still contains visible KO content | Pass |
| M103 LAN/Wi-Fi import format coverage | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-lan-import-formats-device` passed on M103: on-device LAN server stayed on fixed `8787`, required token auth, held Wi-Fi lock, rejected invalid/missing token and unsupported type, accepted Markdown/PDF/EPUB uploads, wrote them local-first as `source=paper_wifi`, then background synced all three to Cloud Hub with stable `doc_id`, MIME type, and blob hash (`doc_71749cf1011b`, `doc_88dd078b38a7`, `doc_e3a08b956705`) | Pass |
| Runtime Sync cursor namespace regression | `npm --workspace ./examples/ai-annotation-demo run smoke:runtime-sync-flow` passed in `17ms`: Web -> Obsidian pulled `evt_smoke_web_pen` with `next_cursor=1`; Obsidian -> Web pulled with `next_cursor=3`, skipped its own earlier event, and preserved controlled `knowledge.update` patch fields (`status=archived`, tags, `task_done=true`) | Pass |
| Cloud Hub session binding and durable store | `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-device-session` passed in `519ms`: session auth is required for Library/Runtime/Knowledge, QR proxy create/status/ack used the shared secret, spoofed tenant/user headers were rejected/overridden by session namespace, and source file/runtime event/ai_turn survived Cloud Hub restart with `runtime_events_after_restart=1` | Pass |
| Meeting SDK event-mark path | `npm --workspace ./examples/ai-annotation-demo run smoke:meeting-v1-e2e` passed in `302ms`: Lark Meeting Timeline SDK accepted 3 single marks and 1 batch mark, Cloud Hub persisted `4 ai_turn`, `4` event KnowledgeObjects, `4` post-process KnowledgeObjects, and `1` meeting DocumentProjection, then rendered Obsidian from persisted Cloud Hub objects into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Meetings/2026-07-03 V1 SDK Meeting E2E/...` | Pass |
| Obsidian plugin and TypeScript checks | `npm run verify:obsidian-v1-plugin` passed source/dist manifest, SDK bundle, V1 runtime text, stale-text absence, and temp-vault install/settings smoke. `npm --workspace ./examples/ai-annotation-demo run check` also passed | Pass |

### Latest Rerun: 2026-07-04 07:40 CST

| Area | Evidence | Result |
| --- | --- | --- |
| V1 integrated E2E after Obsidian vault correction | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` passed in `7773ms` against fixed Cloud Hub `http://127.0.0.1:8731`; session auth was enabled, Web import created `doc_v1_609a602fa7e0`, Library SSE manifest latency was `8ms`, Runtime Sync event `evt_v1_609a602fa7` reached server sequence `259`, and the Obsidian-device pull saw the same event | Pass |
| Cloud Hub LLM gateway and durable knowledge persistence | Same V1 E2E used the Hermes-compatible NodeSK text route (`glm-5.2` through OpenAI chat-completions shape, key loaded from local env only), produced `gateway_error=null`, and persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` for `doc_v1_609a602fa7e0` | Pass |
| Active desktop Obsidian Reading projection | Same V1 E2E rendered `2` Reading files into the deterministic test vault and mirrored the same files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/InkLoop V1 Product E2E 2026-07-03T23-35-46-927Z/...`; plugin install verification returned `active_vault_plugin_installed=true`, and rendered Markdown included the `inkloop://doc/...` backlink plus visible KO content | Pass |
| Web import format coverage on M103 | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-web-import-formats-device` passed on M103 `MBM103YL24L0097`: Web/Cloud Hub-first uploaded Markdown `doc_fmt_markdown_17a2c4b4d9c0`, PDF `doc_fmt_pdf_4eaa058ae2d0`, and EPUB `doc_fmt_epub_e84def13f663`; Cloud Hub manifest preserved MIME/source/blob hash for all 3; Paper Library auto-appeared all 3 in `2356ms` as `source=web`, `sync_status=cloud_only`, `local_available=false`, `cloud_available=true`; each downloaded to local `sync_status=synced`, `local_available=true`, `cloud_available=true` | Pass |
| M103 real-device Cloud Library and mark persistence | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed on M103 `MBM103YL24L0097`: Web-imported `doc_m103_18859df82369` auto-appeared in `796ms` as `cloud_only`, opened/downloaded to `sync_status=synced`, wrote mark `evt_d4801b11` in `386ms`, reopened the same document with `after_reopen_count=1`, restored `strokes=1`, and `inkloop://doc/doc_m103_18859df82369?anchor=evt_d4801b11` returned `anchor_found=true` | Pass |
| M103 post-processing and active Obsidian projection | Same M103 run produced Runtime Sync event `evt_bridge_evt_d4801b11` with `doc_id=doc_m103_18859df82369`, persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` with `gateway_error=null`, then mirrored `2` Reading files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-03T23-30-19-935Z/...` with `active_vault_plugin_installed=true` and visible KO content | Pass |
| Meeting Cloud Hub durable knowledge persistence | `npm --workspace ./examples/ai-annotation-demo run smoke:meeting-v1-e2e` now writes meeting post-process output through Cloud Hub before rendering Obsidian. Latest run persisted `4 ai_turn` records (`result_ann_decision`, `result_ann_action`, `result_ann_risk`, `result_ann_question_batch`), `4` event KnowledgeObjects, `4` post-process KnowledgeObjects, and `1` meeting `DocumentProjection` for `mtgdoc_mtg_v1_sdk_e2e`; it then rendered Obsidian from the objects read back from Cloud Hub (`rendered_from_persisted_cloud_hub_objects=true`) | Pass |
| Active desktop Obsidian Meeting projection | Same meeting run mirrors `InkLoop/Meetings/...` files into `/Users/ethan/Desktop/AI-Content-IP-Vault`; latest run wrote `9` meeting Markdown files under `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Meetings/2026-07-03 V1 SDK Meeting E2E/`, including hub, Decision, Task, Risk, QA, and post-process result notes. `active_vault_contains_source_refs=true` verified visible KnowledgeObject metadata plus structured `document` and `meeting_mark` source refs in the real desktop vault file | Pass |
| Runtime Sync cursor namespace regression | `npm --workspace ./examples/ai-annotation-demo run smoke:runtime-sync-flow` passed in `12ms`: Web -> Obsidian pulled `evt_smoke_web_pen` with `next_cursor=1`; Obsidian -> Web pushed text and controlled `knowledge.update`, pulled with `next_cursor=3`, skipped its own earlier event, and preserved `status=archived`, tags, and `task_done=true` | Pass |
| Cloud Hub session binding and persistence | `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-device-session` passed in `516ms`: Library/Runtime/Knowledge require session, QR proxy create/status/ack used shared secret, spoofed tenant/user headers were overridden/rejected, uploaded source file was visible only through the session namespace, and runtime event plus `ai_turn` survived Cloud Hub restart with `runtime_events_after_restart=1` | Pass |
| Obsidian plugin package regression | `node scripts/verify-obsidian-v1-plugin.mjs` passed after the meeting active-vault mirror change, confirming source/dist manifests, packaged SDK IIFE, V1 runtime text, absent stale text, and temp-vault install/settings smoke | Pass |
| TypeScript checks | `npm --workspace ./examples/ai-annotation-demo run check` passed after adding the M103 PDF/EPUB/Markdown Web-import format coverage smoke | Pass |

### Latest Rerun: 2026-07-04 07:19 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Active Obsidian vault correction | `node scripts/install-obsidian-plugin.mjs --vault /Users/ethan/Desktop/AI-Content-IP-Vault` installed/enabled `inkloop-sync` in the desktop vault used by the current Kickstarter/Hermes docs, fixed Runtime/Knowledge endpoints on `http://127.0.0.1:8731`, and configured a live local Cloud Hub session token without printing it | Pass |
| V1 Cloud Hub E2E with desktop vault projection | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` passed with `session_authenticated=true`; uploaded `doc_v1_47c8afe73780`, source `web`, Library SSE manifest latency `8ms`, Runtime Sync event `evt_v1_47c8afe737` at server sequence `249`, and Obsidian-device pull returned the same event | Pass |
| Cloud Hub LLM gateway and durable post-processing | Same V1 E2E produced `gateway_error=null`, LLM reason `读者直接向AI提出确认请求，要求验证流程闭环，是明确的祈使指令。`, and persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` for `doc_v1_47c8afe73780` | Pass |
| Desktop Obsidian Markdown visibility | Same V1 E2E rendered `2` Reading files into the deterministic test vault and mirrored the same `2` files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/InkLoop V1 Product E2E 2026-07-03T23-19-33-236Z/...`; `active_vault_plugin_installed=true`, `contains_backlink=true`, and `contains_visible_ko_content=true` | Pass |
| M103 Library auto-refresh and mark persistence | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed on M103 `MBM103YL24L0097`: `doc_m103_62717a859ac6` auto-appeared in `760ms`, opened as local synced Markdown, wrote mark `evt_df6c2699` in `333ms`, reopened with `after_reopen_count=1`, restored `strokes=1`, and `inkloop://doc/doc_m103_62717a859ac6?anchor=evt_df6c2699` returned `anchor_found=true` | Pass |
| M103 Cloud/Runtime doc id alignment | Same M103 run produced runtime event `evt_bridge_evt_df6c2699` with `operation=annotation.add`, `mark_id=evt_df6c2699`, and `doc_id=doc_m103_62717a859ac6`, matching the Cloud Library `document_id` | Pass |
| M103 post-processing and desktop Obsidian projection | Same M103 run persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` with `gateway_error=null`; it mirrored `2` Reading files into `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-03T23-19-43-801Z/...`, with `active_vault_plugin_installed=true` and `contains_visible_ko_content=true` | Pass |
| Runtime Sync cursor/session regression | `npm --workspace ./examples/ai-annotation-demo run smoke:runtime-sync-flow` passed with Web -> Obsidian `next_cursor=1`, Obsidian -> Web `next_cursor=3`, no conflicts, and controlled `knowledge.update` patch preserved; `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-device-session` passed with session namespace binding and `runtime_events_after_restart=1` | Pass |
| Obsidian plugin package regression | `node scripts/verify-obsidian-v1-plugin.mjs` passed, confirming source/dist manifests, packaged SDK IIFE, V1 boundary text, Runtime/Knowledge endpoints, namespace cursor migration text, cloud knowledge pull text, and temp-vault install/settings smoke | Pass |
| TypeScript checks | `npm --workspace ./examples/ai-annotation-demo run check` and `npm run check -- --pretty false` passed after the active-vault mirroring changes | Pass |
| M103 stylus preflight with integrated hardware evidence | `ADB=/Users/ethan/Library/Android/sdk/platform-tools/adb M103_STYLUS_INPUT_TIMEOUT_MS=5000 M103_STYLUS_INJECT_SYNTHETIC=1 npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input` passed the synthetic listener check and wrote `examples/ai-annotation-demo/test-results/m103-stylus-input/m103-stylus-input-2026-07-03T23-23-56-592Z.json`. The report now includes `hardware_input_state` before/after capture, `/proc/interrupts` deltas, Huion sysfs, GPIO/pinctrl, handwrite settings, properties, and services. Current synthetic run reported `synthetic_diagnostic_ok=true`, `physical_acceptance_ok=false`, `huion_line_count=14`, `hardware_hgtxx_interrupt_delta=0`, `hardware_fe5a_i2c_interrupt_delta=248`, `system.device_with_handwrite=1`, `sys.is.openhw=1`, `huion_detect_status=1`, `wacom_cast_open=0`, and `work_area_enable=0` | Harness pass; physical stylus still requires live stroke |

### Latest Rerun: 2026-07-04 07:09-07:10 CST

| Area | Evidence | Result |
| --- | --- | --- |
| Obsidian active vault configuration | `node scripts/install-obsidian-plugin.mjs --vault /Users/ethan/Desktop/AI-Content-IP-Vault` installed/enabled `inkloop-sync`, preserved fixed endpoints on `http://127.0.0.1:8731`, wrote `tenantId=local`, `userId=local_demo`, stable `deviceId=obsidian_AI-Content-IP-Vault`, and configured an active Cloud Hub session token without printing it | Pass |
| V1 Cloud Hub E2E under required session auth | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` passed with `session_authenticated=true`; uploaded `doc_v1_d83c9e090014`, source `web`, Library SSE manifest latency `10ms`, Runtime Sync event `evt_v1_d83c9e0900` at server sequence `245`, and Obsidian-device pull returned the same event | Pass |
| Cloud Hub LLM gateway and persistence | Same V1 E2E produced `gateway_error=null`, LLM reason `读者明确提出「请确认…是否形成闭环」，是冲着 AI 的提问/指令。`, and persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` for `doc_v1_d83c9e090014` | Pass |
| M103 Library auto-refresh and mark persistence | `INKLOOP_ACTIVE_OBSIDIAN_VAULT=/Users/ethan/Desktop/AI-Content-IP-Vault npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` passed on M103 `MBM103YL24L0097`: `doc_m103_6a6c6aa60133` auto-appeared in `742ms`, opened as local synced Markdown, wrote mark `evt_4cfc7080`, reopened with `after_reopen_count=1`, restored `strokes=1`, and `inkloop://doc/doc_m103_6a6c6aa60133?anchor=evt_4cfc7080` returned `anchor_found=true` | Pass |
| M103 Cloud/Runtime doc id alignment | Same M103 run produced runtime event `evt_bridge_evt_4cfc7080` with `operation=annotation.add`, `mark_id=evt_4cfc7080`, and `doc_id=doc_m103_6a6c6aa60133`, matching the Cloud Library `document_id` | Pass |
| Active Obsidian visible Markdown projection | Same M103 run rendered `2` Reading files into test vault and mirrored the same files to `/Users/ethan/Desktop/AI-Content-IP-Vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-03T23-10-04-909Z/...`; `contains_visible_ko_content=true`; Obsidian was opened with the generated hub note URI | Pass |
| M103 Huion recovery and synthetic input path | `npm --workspace ./examples/ai-annotation-demo run smoke:m103-input-health:synthetic` passed after Huion driver recovery, found `huion-ts` on `/dev/input/event3`, `DRIVER=Huion-Ts`, `synthetic_diagnostic_ok=true`, `huion_event3_line_count=14`, and HqHw socket log `deliver unified socket stroke` | Harness pass; real physical stylus still requires live stroke |
| M103 physical stylus live preflight | `npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input:live` produced `test-results/m103-stylus-input/m103-stylus-input-2026-07-03T23-12-31-664Z.json`; 45s all-device capture saw `/dev/input/event3` `huion-ts` with `line_count=0`, active device only `gsensor`, `reason=observed_non_stylus_input_only`, `physical_acceptance_ok=false`, `synthetic_diagnostic_ok=false`. Current sysfs exposes `DRIVER=Huion-Ts` and input capabilities but no safe work-area/toggle control under `/sys/bus/i2c/devices/1-0008` beyond driver bind/unbind, so rebind-style experiments should not be repeated for acceptance | Not passed; no real Huion event observed |

| Area | Evidence | Result |
| --- | --- | --- |
| Local Cloud Hub fixed runtime | `com.inkloop.cloudhub.local` launchd now runs `scripts/start-local-cloud-hub-product.ts` on fixed `http://127.0.0.1:8731`; `/healthz` reports `local_device_auth=true` and Library/Runtime/Knowledge `require_session=true`; unauthenticated Library manifest returns `401 missing_session_token` | Pass |
| Web external import is Cloud Hub first | `npm --workspace ./examples/ai-annotation-demo run smoke:v1-product-e2e` uploaded `doc_v1_7d39ccd9793a`, verified manifest, SSE update in `7ms`, blob hash round trip | Pass |
| Paper Library auto-appears and downloads local copy | `npm --workspace ./examples/ai-annotation-demo run smoke:m103-cloud-library-device` on M103 `MBM103YL24L0097` after reinstalling the APK with diagnostic reset + boot-wait physical pen harness; latest Library auto appeared in `736ms`, then `sync_status=synced`, `local_available=true`, `cloud_available=true` for `doc_m103_6a80adcbb936` | Pass |
| Paper mark persistence | Same M103 smoke created `evt_595ba010`, reopened the document, verified `after_reopen_count=1`, restored `strokes=1`, write latency `347ms` | Pass |
| Paper review-later mark persistence | Direct CDP call to `window.__inkloop.markCurrentPageReviewLater()` on M103 created `evt_26a2d47c`; local ledger changed `1 -> 2`, reopening the current book kept `reopened_count=2`; persisted mark fields were `kind=review_later`, `marked_text=稍后处理`, `ai_eligible=false`, `stroke_count=1` | Pass |
| `inkloop://` source open | Same M103 smoke called `window.__inkloop.openInkLoopUri("inkloop://doc/doc_m103_795289a9f8ea?anchor=evt_1e8163c7")`; result `ok=true`, `anchor_found=true`, `active_document_id=doc_m103_795289a9f8ea`, `page_index=0` | Pass |
| Runtime Sync event path | Latest M103 smoke verified runtime event `evt_bridge_evt_595ba010` with operation `annotation.add`; Cloud Library `document_id` and Runtime Sync `doc_id` both equal `doc_m103_6a80adcbb936`; runtime store is persisted at `.inkloop/runtime-events.jsonl` | Pass |
| Runtime Sync cursor namespace/backlog | M103 initially failed on 15 historical recoverable conflicts. `RuntimeStoreInbox` now records recoverable backlog conflicts but advances the namespace cursor when configured by the Paper runtime host; `src/integration/inksurface/runtime-inbox.test.ts` covers both strict no-advance mode and Paper advance mode; latest M103 smoke passed after the fix | Pass |
| Review-later Runtime Sync projection | After the builder fix and Cloud Hub restart, runtime pull for `evt_bridge_evt_26a2d47c` returns exactly one event with `annotation.kind=reading_note`, `annotation.title=稍后处理`, `mark_id=evt_26a2d47c` | Pass |
| Cloud post-processing persistence | V1 and M103 smokes rely on Cloud Hub automatic post-processing from accepted `annotation.add`; latest M103 record persisted exactly `1 ai_turn`, `1 KnowledgeObject`, and `1 DocumentProjection` for `doc_m103_6a80adcbb936`; V1 E2E also persisted the same triplet for `doc_v1_7d39ccd9793a` | Pass |
| Cloud post-processing for review-later | Cloud Hub persisted `turn_evt_bridge_evt_26a2d47c` and `ko_VXK7JQPT0869YF2FT9ZR1F4C55`; KO is `kind=reading_note`, `title=稍后处理`, `status=inbox`, `mark_ids=[evt_26a2d47c]`, and `llm_error=null` | Pass |
| Obsidian Reading projection | Rendered V1 and M103 Markdown projections are generated under `InkLoop/Reading/<source-file>/...`; latest M103 smoke produced `test-results/m103-real-device-e2e-vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-03T21-57-56-649Z/Ink mark - evt_595b.md`, with `rendered_file_count=2` and `contains_visible_ko_content=true`. The same 2 Reading files were incrementally mirrored into the active iCloud vault at `/Users/ethan/Library/Mobile Documents/iCloud~md~obsidian/Documents/knowledge-vault/InkLoop/Reading/M103 Cloud Library Real Device E2E 2026-07-03T21-57-56-649Z/...` | Pass |
| Obsidian plugin live vault | `inkloop-sync` installed/enabled in the active `knowledge-vault`; installed plugin settings point to Runtime Sync and Cloud Knowledge on fixed `http://127.0.0.1:8731`, include a live session token, and use device id `obsidian-active-vault`; latest M103 E2E hub note was opened through an `obsidian://open` URI | Pass |
| Paper LAN/Wi-Fi import service | `npm --workspace ./examples/ai-annotation-demo run smoke:m103-lan-import-device` verified direct same-Wi-Fi upload to `http://172.168.20.91:8787`, transport `direct_lan`, token enforcement `403/403`, unsupported type `415`, upload `200`, `wifi_lock_held=true`, QR URL visible in UI, local-first open, source `paper_wifi`, Cloud Hub session-auth manifest inclusion, and final `sync_status=synced` for `doc_5d338af18db7` | Pass |
| Meeting SDK axis and marks | `npm --workspace ./examples/ai-annotation-demo run smoke:meeting-v1-e2e` started Lark Meeting Timeline SDK server, opened meeting axis, wrote 3 single annotations and 1 batch annotation; latest SDK latencies were `3ms/1ms/1ms`, batch `1ms`, SSE latency `4ms`, total smoke `219ms` | Pass |
| Lark Meeting Timeline SDK checks | `npm run check --prefix examples/ai-annotation-demo/Lark-Meeting-Timeline-main` passed the local SDK contract suite, including device ingest, annotation routing, open meeting session, SSE, event log persistence, raw WS diagnostics, and meeting search diagnostics | Pass |
| Meeting schema alignment | Same meeting smoke produced `4` aligned events, `4` document refs, `4` meeting mark refs, and `1` project memory ref in `PostProcessContext` | Pass |
| Meeting postprocess source_refs | Same meeting smoke generated contract KnowledgeObjects for `meeting_decision`, `meeting_action`, `meeting_risk`, and `qa`, and rendered structured document + meeting_mark + project_memory `source_refs` into Obsidian Markdown | Pass |
| Obsidian plugin package | `node scripts/verify-obsidian-v1-plugin.mjs` passed after rebuild and confirmed source/dist manifests, packaged SDK IIFE, required V1 runtime text, absent stale text, and temp-vault install/settings smoke | Pass |
| SDK package / types | `npm run check`, `npm test`, `npm run build`, and `node scripts/verify-package-consumer.mjs` passed | Pass |
| Android Paper APK | `npm run android:assemble:debug`, `adb install -r .../app-debug.apk`, and `adb shell am start -n com.inkloop.app/.MainActivity` completed before the latest M103 smoke; build uses product device-session mode | Pass |
| Cloud Hub LLM gateway | Cloud Hub standalone loads `~/.hermes/.env`, maps `NODESK_API_KEY` to `LLM_GATEWAY_KEY`, and uses `glm-5.2` via OpenAI chat-completions route. E2E now fails if `metadata.llm_error` exists; latest Cloud Hub local-auth and M103 runs reported `gateway_error=null` | Pass |
| Cloud Hub multi-user isolation | `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-multi-user` passed against live `http://127.0.0.1:8731`; same `doc_id`, `event_id`, `ai_turn_id`, `ko_id`, and `projection_id` were written under `tenant_iso_mr5em955_jq7gr7/user_a` and `user_b`, while `user_c` saw `0` runtime/knowledge records; Library SSE/blob did not leak across users | Pass |
| Cloud Hub device session binding | `npm --workspace ./examples/ai-annotation-demo run smoke:cloud-hub-device-session` passed in `616ms` with a local mock Panel Auth service: unauthenticated Library returned `401`, QR proxy create/status/ack returned a session, session auth overrode spoofed tenant/user/device headers, header-only spoofing was rejected, and Library/Runtime/Knowledge records survived Cloud Hub restart | Pass |
| M103 stylus low-level input monitor | `npm --workspace ./examples/ai-annotation-demo run smoke:m103-stylus-input` is now available as a small preflight before full WebView/CDP physical pen capture, `smoke:m103-stylus-input:live` gives a 45s operator window for real-stylus acceptance, and `smoke:m103-stylus-input:synthetic` is a low-level listener self-test that must not count as physical acceptance. The report now includes explicit operator instructions, `capture_window`, `requires_real_physical_stylus=true`, `physical_acceptance_ok`, `synthetic_diagnostic_ok`, `huion_line_count`, `non_stylus_active_devices`, and a concrete `next_action`. It clears logcat before the capture window and records `capture_logcat_relevant_lines` separately, so stale HqHw logs do not pollute live evidence. It defaults to all-device capture and writes `test-results/m103-stylus-input/*.json`; `M103_STYLUS_INPUT_DEVICE=huion-ts` or `stylus` can narrow the target. Latest live check `m103-stylus-input-2026-07-03T22-40-03-205Z.json` found `huion-ts` on `/dev/input/event3` with touch/stylus/eraser/X/Y/pressure capability, confirmed adb root context, SELinux `Enforcing`, `/dev/input/event3` permissions, sysfs snapshot (`PRODUCT=18/dead/beef/28bb`, `PROP=2`, `EV=b`, `ABS=d000003`, runtime power unsupported), `/proc/bus/input/devices` huion block, and `dumpsys input` huion/stylus lines. The 45s clean capture observed only `gsensor` lines on `/dev/input/event2` (`line_count=1416`), `capture_logcat_relevant_lines=[]`, and returned `physical_acceptance_ok=false`, `synthetic_diagnostic_ok=false`, `reason=observed_non_stylus_input_only`, `huion_line_count=0`, `non_stylus_active_devices=[gsensor]`; this is a valid harness result but not physical stylus acceptance. Synthetic self-test `m103-stylus-input-2026-07-03T22-39-47-354Z.json` passed with `physical_acceptance_ok=false`, `synthetic_diagnostic_ok=true`, `huion_line_count=14`, `BTN_TOUCH=2`, `ABS_X=2`, `ABS_Y=2`, and `ABS_PRESSURE=3`, proving the low-level listener can observe `/dev/input/event3` when events are emitted | Harness ready and self-tested; needs real stylus stroke to pass |
| M103 input health / hardware interrupt evidence | `npm --workspace ./examples/ai-annotation-demo run smoke:m103-input-health` now records `getevent -lp`, `/sys/class/input/event3`, `/sys/bus/i2c/devices/1-0008`, relevant properties/services, Huion sysfs state (`huion_detect_status`, `wacom_cast_open`, `work_area_*`, wakeup counters), `getevent` captures for `event3` and `event2`, `/proc/interrupts` deltas for `hgtxx`, `fts_ts`, and I2C lines, plus debugfs GPIO/pinctrl and `/proc/irq/86/spurious`. Latest short live health report `m103-input-health-2026-07-03T22-58-54-579Z.json` returned `reason=no_real_huion_event3_input`, `physical_acceptance_ok=false`, `huion_event3_line_count=0`, `gsensor_event2_line_count=168`, `hgtxx_interrupt_delta=0`, and `fe5a_i2c_interrupt_delta=256`; the device still exposes `DRIVER=Huion-Ts`, `OF_COMPATIBLE_0=huion,hgtxx`, and `haoqingdrawserver` is running. The live debugfs snapshot shows `HW_EMR_INT_IRQ` on `gpio-14` remains `in hi`, pinconf is `input bias pull up`, pinmux is `(MUX UNCLAIMED) gpio0:14`, `/proc/irq/86/spurious` is `count 644 / unhandled 0`, and Huion sysfs stayed `huion_detect_status=1`, `wacom_cast_open=0`, `work_area_enable=0`, `wakeup_event_count=0`. Latest synthetic health report `m103-input-health-2026-07-03T22-58-30-874Z.json` returned `synthetic_diagnostic_ok=true`, `huion_event3_line_count=14`, `hgtxx_interrupt_delta=0`, the same Huion/debugfs fields, and HqHw/haoqingdrawserver socket logs including `deliver unified socket stroke`, proving the event3 -> HqHw socket -> user-space diagnostic path works while physical GPIO interrupt evidence remains absent | Harness ready and self-tested; physical hgtxx interrupt/real stylus still absent |
| M103 physical pen capture bridge | `npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-capture` now waits for cold-start JS bridge readiness, resets native diagnostics before capture, records `/dev/input` device capabilities, starts a parallel all-device low-level `getevent` capture by default (`M103_PEN_LOW_LEVEL_INPUT_DEVICE=all`), verifies writable canvas and HqHw readiness, then waits for a real stylus stroke. The report includes structured `diagnosis.stage` / `diagnosis.reason`: `captured`, `readiness`, `low_level_input`, or `app_bridge`, plus `physical_acceptance_ok` and `synthetic_diagnostic_ok`. Latest readiness check `m103-physical-pen-capture-2026-07-03T22-57-24-486Z.json` opened `doc_m103_6a80adcbb936`, verified `ready=true`, `osd_armed=true`, `InkLoopHqHwArea=true`, `InkLoopInputSource=true`, visible writable `#ink-layer`, active `HqHwBridge` area `[147,78][1386,1158]`, `socket_connected=true`, and zero input/app packets after diagnostics reset; the 1s non-synthetic capture still saw only `gsensor` and returned `reason=no_low_level_stylus_event`. Synthetic downstream diagnostic is standardized as `npm --workspace ./examples/ai-annotation-demo run smoke:m103-physical-pen-synthetic`; latest report `m103-physical-pen-capture-2026-07-03T22-24-45-316Z.json` captured `17` RawPenFrames via `m103_hqunifiedsocket`, HqHw socket packet count `17`, and `evidence:ai-pen-run` passed with schema pass rate `1`, complete stroke, and host latency p50/p95 `12ms`. Synthetic reports set `physical_acceptance_ok=false` and `synthetic_diagnostic_ok=true` | Bridge/socket/fallback downstream path proven by synthetic event injection; app readiness proven; real physical RawPenFrame still not accepted because no real low-level stylus event has been observed |

## P0 Requirement State

| P0 | Requirement | Current State |
| --- | --- | --- |
| P0-1 | Web file import makes Paper Library visible | Strong evidence on M103 for all v1 demo formats: Markdown, PDF, and EPUB uploaded Cloud Hub-first from the Web side, appeared in Paper Library as `cloud_only`, and downloaded to local `synced` without manual refresh. Latest real-device Web-import run auto-appeared in `920ms` with no manual inbox refresh |
| P0-2 | Paper Wi-Fi import fixed `8787` + token + local-first + retry | Strong evidence on M103 for Markdown, PDF, and EPUB via the on-device LAN server: fixed `8787`, token/security checks, unsupported type rejection, `paper_wifi` source, local-first availability, Cloud Hub background sync, stable `doc_id`, session-auth manifest visibility, and `wifi_lock_held=true` |
| P0-3 | Source-file Library with reading/download/sync state | Strong evidence for cloud_only -> synced on M103 |
| P0-4 | Paper reading marks do not interrupt reading and do not disappear | Strong evidence for persisted reading mark semantics on M103: `smoke:m103-reading-mark-types-device` now creates `underline`, `circle`, `handwriting`, and `review_later` through the Paper runtime, verifies they survive reopening the source file with strokes intact, and verifies all four reach Cloud Hub Runtime Sync as `annotation.add` events under the same `doc_id`. M103 physical pen export harness is installed, writable canvas is visible, HqHw is armed, MotionEvent fallback capture is installed, default smoke no longer runs an ADB input probe before real capture, per-run diagnostic counters reset to zero, and the harness captures all-device low-level input evidence alongside App bridge evidence. Synthetic event injection proves both the low-level listener and downstream RawPenFrame path can observe `/dev/input/event3` and export valid JSONL with p50/p95 host latency `12ms`, but current non-synthetic classification is still `no_low_level_stylus_event`/`observed_non_stylus_input_only`; real low-latency pen-path latency remains unproven until a run observes physical stylus input or hgtxx interrupt movement |
| P0-5 | Meeting/classroom event marks land on meeting axis | Strong evidence via Lark Meeting Timeline SDK E2E |
| P0-6 | Runtime sync incremental sync, user/session isolation | Runtime event sync, persistent JSONL store, Obsidian namespace cursor path, live Cloud Hub namespace isolation, and Cloud Hub session binding are verified. When session auth is required, Library/Runtime/Knowledge reject unauthenticated calls and use the introspected session namespace instead of spoofable headers. Runtime Sync still supports explicit dev namespace headers only when session enforcement is disabled |
| P0-7 | Obsidian Reading Note / Highlight / Task / Decision / Risk | Strong evidence for all five Reading projection kinds: `smoke:obsidian-reading-kind-coverage` renders `reading_note`, `highlight`, `task`, `decision`, and `risk` plus the source-file hub into both test vault and `/Users/ethan/Desktop/AI-Content-IP-Vault`, and verifies the expected Markdown callouts plus controlled-field parsing |
| P0-8 | Obsidian controlled writeback for task/status/tag/risk fields | Implemented and verified: Obsidian Markdown now renders `Controlled Fields`, plugin parses only `<!-- inkloop:controlled-fields v1 -->`, pushes `knowledge.update`, Cloud Hub persists status/tags/task/risk/comment fields into `KnowledgeObject.controlled_fields` |
| P0-9 | `inkloop://` source links return to original location | Implemented and verified on M103 through `window.__inkloop.openInkLoopUri(...)`: Cloud Library lookup/download, open source doc, resolve anchor from local mark ledger, and jump to page |
| P0-10 | Cloud Hub device status / DeviceManifest | Implemented and verified: Paper app sends session-authenticated `/v1/devices/heartbeat`, Cloud Hub persists per-user `DeviceManifest`, spoofed headers/body cannot move a device into another namespace, live M103 reports online status plus LAN/import/runtime context, and records survive Cloud Hub restart |

## Next Highest-Risk Work

1. Run `smoke:m103-stylus-input:live` while drawing one long real M103 stylus stroke. This is the fastest preflight for proving the pen is producing `/dev/input/event3` events, and it now reports both low-level `getevent` evidence and hardware evidence in one file: `capture_window`, `huion_line_count`, non-stylus active devices, `/proc/interrupts` deltas, Huion sysfs, GPIO/pinctrl, handwrite settings, properties, and `next_action`. `smoke:m103-stylus-input:synthetic` proves the listener and HqHw socket path can observe injected `/dev/input/event3` events, but it must not be counted as physical acceptance because `physical_acceptance_ok=false`. If a non-synthetic live run still has `huion_line_count=0` and `hardware_hgtxx_interrupt_delta=0`, the failure is before Android input delivery and points at the physical stylus/digitizer wake/EMR interrupt line. If `hardware_hgtxx_interrupt_delta>0` but `huion_line_count=0`, inspect Huion driver event reporting and input device registration. If `huion_line_count>0`, run `smoke:m103-physical-pen-capture` with the same real stroke to verify WebView bridge, RawPenFrame export, and latency. Use `M103_INPUT_DISPATCH_PROBE=1` only for diagnosing WebView input dispatch. Use `smoke:m103-physical-pen-synthetic` only for downstream bridge diagnostics; it must not be counted as physical acceptance because `physical_acceptance_ok=false`. If real low-level stylus counters increase but HqHw/InputSource/RawPenFrame remain zero, inspect `/tmp/hqunifiedsocket`, MotionEvent dispatch, and coordinate filtering; if it exports JSONL, run `npm --workspace ./examples/ai-annotation-demo run evidence:ai-pen-run -- <raw-jsonl>`.
2. Run a real QR/login proof against the production panel auth service; current evidence proves Cloud Hub session binding with a mock Panel Auth service, not the human扫码 onboarding UX.
3. Keep T10C-specific low-latency rendering work separate from this M103 V1 demo path until its device driver path is confirmed.
